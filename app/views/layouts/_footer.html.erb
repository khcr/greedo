<footer>
  <% if current_user.nil? %>
    <p><%= link_to "Login", login_path %></p>
  <% else %>
    <p>
      <%= link_to current_user.name, profile_path %> | 
      <%= link_to "Déconnecter", signout_path %>
    </p>
  <% end %>
</footer>